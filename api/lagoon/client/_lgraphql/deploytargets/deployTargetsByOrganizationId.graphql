query (
  $id: Int!) {
    organizationById(id: $id) {
      deployTargets {
        id
        name
        created
        cloudRegion
        cloudProvider
        friendlyName
        sshHost
        sshPort
        routerPattern
        buildImage
      }
    }
  }
