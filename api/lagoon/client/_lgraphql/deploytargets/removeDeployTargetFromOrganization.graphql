mutation (
  $deployTarget: Int!,
  $organization: Int!,){
  removeDeployTargetFromOrganization(input: {
    deployTarget: $deployTarget,
    organization: $organization,
  }) {
    id
    name
  }
}
