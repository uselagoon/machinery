mutation (
    $id: Int!
    $services: [String]!){
    setEnvironmentServices(
        input: {
            environment: $id
            services: $services
        }
    ){
        id
        name
    }
}