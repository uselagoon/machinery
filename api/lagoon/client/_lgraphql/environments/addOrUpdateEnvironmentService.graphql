mutation (
  $service: AddEnvironmentServiceInput!) {
    addOrUpdateEnvironmentService(input: $service) {
        id
        name
        type
        containers{
            name
        }
        created
        updated
    }
}
