mutation (
  $volume: AddEnvironmentVolumeInput!) {
    addOrUpdateEnvironmentVolume(input: $volume) {
        id
        name
        type
        storageType
        kibRequested
        abandoned
        created
        updated
    }
}
