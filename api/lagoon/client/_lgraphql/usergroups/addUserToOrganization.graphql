mutation (
  $user: UserInput!,
  $organization: Int!,
  $owner: Boolean) {
    addUserToOrganization(input: {
      user: $user
      organization: $organization
      owner: $owner
    }) {
      id
      name
    }
  }
