query groupByName ($name: String!) {
    groupByName (name: $name) {
        id
        name
        members{
            user{
                email
                firstName
                lastName
                comment
                gitlabId
            }
            role
        }
    }
}
