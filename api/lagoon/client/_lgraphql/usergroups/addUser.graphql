mutation (
  $email: String!,
  $firstName: String,
  $lastName: String,
  $comment: String,
  $resetPassword: Boolean,
  $gitlabId: Int) {
    addUser(input: {
      email: $email
      firstName: $firstName
      lastName: $lastName
      comment: $comment
      resetPassword: $resetPassword
      gitlabId: $gitlabId
    }) {
      id
      email
      firstName
      lastName
    }
  }
