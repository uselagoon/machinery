query allUsers(
    $email: String
    $id: String
    $gitlabId: Int
) {
    allUsers(
        email: $email
        id: $id
        gitlabId: $gitlabId
    ) {
        id
        email
        firstName
        lastName
        comment
    }
}