query deploymentByRemoteId($remoteId: String!) {
    deploymentByRemoteId(id: $remoteId) {
        id
        name
        status
        created
        started
        completed
        remoteId
        uiLink
        priority
        bulkId
        bulkName
        buildStep
        environment {
            name
        }
    }
}