mutation (
  $name: String!,
  $webhook: String!,
  $organization: Int) {
    addNotificationMicrosoftTeams(input: {
      name: $name,
      webhook: $webhook,
      organization: $organization
    }) {
      id
      name
      webhook
      organization
    }
  }
