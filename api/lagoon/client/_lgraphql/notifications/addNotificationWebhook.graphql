mutation (
  $name: String!,
  $webhook: String!,
  $organization: Int) {
    addNotificationWebhook(input: {
      name: $name,
      webhook: $webhook,
      organization: $organization
    }) {
      id
      name
      webhook
      organization
    }
  }
