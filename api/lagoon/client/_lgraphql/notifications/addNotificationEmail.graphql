mutation (
  $name: String!,
  $emailAddress: String!
  $organization: Int) {
    addNotificationEmail(input: {
      name: $name,
      emailAddress: $emailAddress,
      organization: $organization
    }) {
      id
      name
      emailAddress
      organization
    }
  }
